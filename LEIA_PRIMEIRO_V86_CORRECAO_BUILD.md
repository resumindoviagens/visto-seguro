# V86 — Correção de build da V85

Corrige erro de sintaxe em `lib/emailTemplates.js`.

O modelo `foto_instrucoes` havia sido inserido fora do objeto `templates`.

## SQL

Se o SQL da V85 já foi executado, não precisa executar novamente.
