# V87 — Correção definitiva do build

Corrige definitivamente o arquivo:

`lib/emailTemplates.js`

O modelo `foto_instrucoes` foi movido para dentro do objeto `templates`.

## SQL

Não precisa executar SQL novo se o SQL da V85 já foi executado.
