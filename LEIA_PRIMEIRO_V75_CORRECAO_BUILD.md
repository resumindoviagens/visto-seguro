# V75 — Correção de build da V74

Corrige erro de importação em:

`app/api/admin/feedbacks/route.js`

## SQL

Se o SQL da V74 já foi executado, não precisa executar novamente.

Se ainda não executou, use:

`supabase/migrations/20260510_v74_etapa1_email_feedback_alertas.sql`
